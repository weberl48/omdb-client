var input = "how now brown cow"

function (input){
  var result = {};
  for(var i=0; i<input.length; i++) {
    result[input[i]] = result[input[i]] || 0;
    result[input[i]] += 1;
  }
  return result;
}
